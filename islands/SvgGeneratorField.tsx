import {Settings} from '../image_generator/const/Settings.ts';

interface SvgGeneratorProps {
  settings: Settings
}

export default function SvgGeneratorField({ settings } : SvgGeneratorProps) {
  return (
    <div>

    </div>
  );
}